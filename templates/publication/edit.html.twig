{% extends 'base.html.twig' %}

{% block title %}Modifier Publication{% endblock %}

{% block body %}
    <h1>Modifier la Publication  {{ publication.id}}</h1>

    {{ include('publication/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('publication_index') }}">Retour</a>

    {{ include('publication/_delete_form.html.twig') }}
{% endblock %}
